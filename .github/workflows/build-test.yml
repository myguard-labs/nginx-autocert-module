name: Build & Test

# Third-party actions pinned to commit SHAs for supply-chain safety.
# Pin table (canonical for all workflow files — keep consistent):
# actions/checkout@v5          -> 93cb6efe18208431cddfb8368fd83d5badbf9bfd
# actions/cache@v4             -> 0057852bfaa89a56745cba8c7296529d2fc39830
# actions/upload-artifact@v5   -> 330a01c490aca151604b8cf639adc76d48f6c5d4
# actions/download-artifact@v5 -> 634f93cb2916e3fdff6788551b99b062d0335ce0
#
# Local composite actions:
#   ./.github/actions/setup         apt deps + optional ccache restore.
#   ./.github/actions/build-module  fetch+build one flavor, run the no-network
#                                   load/serve tests, upload build-<flavor>.
#   ./.github/actions/run-e2e       restore build-nginx + run one tests/e2e/*.sh
#                                   Pebble e2e (optionally under sudo).

# Neither nginx nor angie is pinned: the `resolve` job scrapes the current
# mainline nginx and the latest angie release at run time.
#   - build-nginx and build-angie both fully build the flavor + the module, run
#     the no-network load/serve/lifecycle/USR2 tests, and upload the tree as
#     build-<flavor>. The unit jobs download build-nginx (the C they test is
#     flavor-independent); the Pebble e2e suite runs against BOTH flavors via a
#     matrix. angie ships its own native `acme` directive, but this module's
#     `autocert` directive is independent and coexists with it — so the module
#     is fully supported + tested on angie, not just compiled. (Reverses the
#     earlier compile-only stance, PR #26 — see history.md 2026-06-20.)

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch: {}

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:

  # Fast lint gate (adapted from nginx-error-abuse-module): static-check the C,
  # the shell test harness and the workflows before the build matrix spins up.
  validation:
    name: Validation
    runs-on: [self-hosted, builder02, lxc]
    timeout-minutes: 15
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Install validation tools
        run: |
          sudo apt-get update
          sudo apt-get install -y cppcheck shellcheck

      - name: Validate scripts and source
        run: |
          bash -n tests/e2e/*.sh tests/fuzz/*.sh tests/unit/*.sh
          shellcheck tests/e2e/*.sh tests/fuzz/*.sh tests/unit/*.sh
          # Reject literal NUL bytes in tracked C sources: they make git/rg treat
          # the file as binary and silently drop it from source search / review.
          if LC_ALL=C grep -laP '\x00' src/*.c src/*.h; then
            echo "::error::NUL byte in tracked C source (listed above)"; exit 1
          fi
          # Tell cppcheck the OpenSSL version so it takes the satisfied branch
          # of the >= 3.0.0 floor guard instead of evaluating the intentional
          # `#error` with the macro undefined (cppcheck doesn't parse the system
          # opensslv.h). The real build enforces the same floor at compile time.
          cppcheck \
            --enable=warning,performance,portability \
            --error-exitcode=1 \
            -DOPENSSL_VERSION_NUMBER=0x30000000L \
            --suppress=missingIncludeSystem \
            --suppress=normalCheckLevelMaxBranches \
            --suppress=unusedFunction \
            src/*.c
          git diff --check

      - name: Lint workflows
        run: |
          version=1.7.7
          base="https://github.com/rhysd/actionlint/releases/download/v${version}"
          tarball="actionlint_${version}_linux_amd64.tar.gz"
          # Verify the asset against the checksums published in the same release
          # before executing it, so a tampered download fails closed.
          curl -fsSL -o /tmp/actionlint.tgz "${base}/${tarball}"
          curl -fsSL -o /tmp/actionlint.checksums \
            "${base}/actionlint_${version}_checksums.txt"
          ( cd /tmp && grep " ${tarball}\$" actionlint.checksums \
              | sed 's#'"${tarball}"'#actionlint.tgz#' | sha256sum -c - )
          tar -xzf /tmp/actionlint.tgz -C /tmp actionlint
          /tmp/actionlint -color .github/workflows/*.yml

  resolve:
    name: Resolve nginx + angie versions
    runs-on: [self-hosted, builder02, lxc]
    timeout-minutes: 5
    outputs:
      nginx_version: ${{ steps.v.outputs.nginx_version }}
      nginx_dir: ${{ steps.v.outputs.nginx_dir }}
      nginx_url: ${{ steps.v.outputs.nginx_url }}
      angie_version: ${{ steps.v.outputs.angie_version }}
      angie_dir: ${{ steps.v.outputs.angie_dir }}
      angie_url: ${{ steps.v.outputs.angie_url }}
    steps:
      - name: Resolve latest mainline nginx + latest angie
        id: v
        run: |
          set -euo pipefail
          ver=$(curl -fsSL https://nginx.org/en/download.html \
            | grep -oP 'nginx-\K[0-9]+\.[0-9]+\.[0-9]+(?=\.tar\.gz)' \
            | sort -V | tail -1)
          if [ -z "$ver" ]; then
            echo "::error::could not resolve mainline nginx version"; exit 1
          fi
          angie=$(curl -fsSL https://download.angie.software/files/ \
            | grep -oP 'angie-\K[0-9]+\.[0-9]+\.[0-9]+(?=\.tar\.gz)' \
            | sort -V | tail -1)
          if [ -z "$angie" ]; then
            echo "::error::could not resolve latest angie version"; exit 1
          fi
          echo "Resolved nginx mainline: $ver  angie: $angie"
          {
            echo "nginx_version=$ver"
            echo "nginx_dir=nginx-$ver"
            echo "nginx_url=https://nginx.org/download/nginx-$ver.tar.gz"
            echo "angie_version=$angie"
            echo "angie_dir=angie-$angie"
            echo "angie_url=https://download.angie.software/files/angie-$angie.tar.gz"
          } >> "$GITHUB_OUTPUT"

  build-nginx:
    name: Build & load (nginx ${{ needs.resolve.outputs.nginx_version }})
    needs: resolve
    runs-on: [self-hosted, builder02, lxc]
    timeout-minutes: 20
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: Build module + load/serve tests
        uses: ./.github/actions/build-module
        with:
          flavor: nginx
          version: ${{ needs.resolve.outputs.nginx_version }}
          url: ${{ needs.resolve.outputs.nginx_url }}
          dir: ${{ needs.resolve.outputs.nginx_dir }}

  # angie ships its own native `acme` directive, but this module's `autocert`
  # directive is independent and coexists with it (the operator chooses), so we
  # build + run the FULL no-network suite on angie just like nginx and publish a
  # build-angie artifact for the Pebble e2e matrix. (Reverses PR #26's
  # compile-only stance — see history.md 2026-06-20.)
  build-angie:
    name: Build & load (angie ${{ needs.resolve.outputs.angie_version }})
    needs: resolve
    runs-on: [self-hosted, builder02, lxc]
    timeout-minutes: 20
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: Build module + load/serve tests
        uses: ./.github/actions/build-module
        with:
          flavor: angie
          version: ${{ needs.resolve.outputs.angie_version }}
          url: ${{ needs.resolve.outputs.angie_url }}
          dir: ${{ needs.resolve.outputs.angie_dir }}

  # Coexistence: this module's `autocert` alongside Angie's native `acme`
  # (the implicit "@acme" server block) in one instance — the exact prod
  # scenario that SIGSEGV'd in serve_init (memory issues.md 2026-06-22). angie
  # only (the build sets --with-http_acme_module for angie); the test skips on
  # nginx. `-t` only, no docker.
  coexist-native-acme:
    name: Coexist with native angie acme (-t)
    needs: [resolve, build-angie]
    runs-on: [self-hosted, builder02, docker]
    timeout-minutes: 15
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: native acme + autocert in one instance
        uses: ./.github/actions/run-e2e
        with:
          flavor: angie
          dir: ${{ needs.resolve.outputs.angie_dir }}
          script: coexist-native-acme.sh
          # native acme's implicit @acme block binds :80 for http-01 even under
          # `-t`; the unprivileged runner needs sudo for the bind to succeed.
          sudo: "true"

  # ABI-drift regression. The prod crash was a struct-offset mismatch: the .so
  # was built against vanilla angie, but our packaged angie carries
  # nginx_dynamic_tls_records (adds ngx_ssl_dyn_rec_t to ngx_ssl_s, +32B,
  # shifting ngx_http_ssl_srv_conf_t.certificates 208->240). This builds the
  # module against vanilla AND a synthetically-enlarged ngx_ssl_s tree and
  # proves: matched builds work, mismatched builds are refused by the module's
  # ABI guard (clean [emerg], never SIGSEGV). Self-contained — no dependency on
  # our private deb patches.
  abi-mismatch-guard:
    name: ABI-drift guard (matched OK, mismatch refused)
    needs: resolve
    runs-on: [self-hosted, builder02, lxc]
    timeout-minutes: 25
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: Build vanilla + enlarged angie, run ABI matrix
        shell: bash
        env:
          ANGIE_URL: ${{ needs.resolve.outputs.angie_url }}
        run: |
          set -euo pipefail
          WORKSPACE="$GITHUB_WORKSPACE" bash tests/e2e/abi-build-and-check.sh

  # Prove a PRODUCTION build (no AUTOCERT_TEST) omits the test-only seed
  # directives entirely, so a stray/templated autocert_test_challenge cannot
  # inject a forged key authorization. Builds the module withOUT the flag and
  # asserts `nginx -t` rejects the directive as unknown.
  test-directives-guard:
    name: Test-only directives rejected in prod build
    needs: resolve
    runs-on: [self-hosted, builder02, lxc]
    timeout-minutes: 20
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Setup build deps
        uses: ./.github/actions/setup
        with:
          ccache-key: build-nginx-${{ needs.resolve.outputs.nginx_version }}

      - name: Restore nginx tarball cache
        id: nginx-tarball
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: ~/.cache/src-tarballs
          key: src-tarball-nginx-${{ needs.resolve.outputs.nginx_version }}

      - name: Fetch nginx ${{ needs.resolve.outputs.nginx_version }}
        # Do NOT gate on cache-hit: on self-hosted runners actions/cache can
        # report a hit while the path is empty, so always ensure the file is
        # actually present (idempotent — curl only when missing).
        run: |
          set -euo pipefail
          mkdir -p ~/.cache/src-tarballs
          tarball=~/.cache/src-tarballs/nginx-${{ needs.resolve.outputs.nginx_version }}.tar.gz
          if [ ! -s "$tarball" ]; then
            curl -fsSL -o "$tarball" "${{ needs.resolve.outputs.nginx_url }}"
          fi

      - name: Extract nginx ${{ needs.resolve.outputs.nginx_version }}
        run: |
          set -euo pipefail
          tar -xzf ~/.cache/src-tarballs/nginx-${{ needs.resolve.outputs.nginx_version }}.tar.gz

      - name: Build prod (no AUTOCERT_TEST) + assert directive rejected
        run: |
          set -euo pipefail
          cd "${{ needs.resolve.outputs.nginx_dir }}"
          # No AUTOCERT_TEST in the env: the test-only directives must be absent.
          ./configure --with-compat --with-http_ssl_module \
            --with-cc="ccache cc" \
            --with-cc-opt="-Wall -Wextra -Werror" \
            --add-dynamic-module="$GITHUB_WORKSPACE" > /tmp/conf.log 2>&1 \
            || { tail -40 /tmp/conf.log; exit 1; }
          # Full build: `nginx -t` below needs the server binary, not just the .so.
          make > /tmp/make.log 2>&1 || { tail -40 /tmp/make.log; exit 1; }
          test -f objs/ngx_http_autocert_module.so
          BIN=objs/nginx
          test -x "$BIN"
          PREFIX=/tmp/guard; mkdir -p "$PREFIX/conf" "$PREFIX/logs"
          for dir in autocert_test_challenge autocert_test_alpn; do
            cat > "$PREFIX/conf/nginx.conf" <<EOF
          load_module $PWD/objs/ngx_http_autocert_module.so;
          events {}
          http { $dir a b; server { listen 8099; autocert on; } }
          EOF
            out="$("$BIN" -t -p "$PREFIX" -c "$PREFIX/conf/nginx.conf" 2>&1 || true)"
            if printf '%s\n' "$out" | grep -q "unknown directive \"$dir\""; then
              echo "✓ prod build rejects $dir"
            else
              echo "::error::$dir was NOT rejected by a no-flag prod build"
              printf '%s\n' "$out" | sed 's/^/    /'
              exit 1
            fi
          done
          ccache -s || true

  crypto-unit:
    name: Crypto unit tests (M3)
    needs: [resolve, build-nginx]
    runs-on: [self-hosted, builder02, lxc]
    timeout-minutes: 10
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Setup build deps
        uses: ./.github/actions/setup
        with:
          packages: build-essential libssl-dev

      - name: Download nginx build
        uses: actions/download-artifact@634f93cb2916e3fdff6788551b99b062d0335ce0 # v5
        with:
          name: build-nginx
          path: ${{ needs.resolve.outputs.nginx_dir }}

      - name: Build + run crypto unit tests
        run: |
          set -euo pipefail
          NGX="$GITHUB_WORKSPACE/${{ needs.resolve.outputs.nginx_dir }}"
          INC="-I$NGX/src/core -I$NGX/src/event -I$NGX/src/os/unix -I$NGX/objs"
          OBJS="$NGX/objs/src/core/ngx_palloc.o $NGX/objs/src/core/ngx_string.o \
                $NGX/objs/src/os/unix/ngx_time.o $NGX/objs/src/core/ngx_times.o \
                $NGX/objs/src/os/unix/ngx_alloc.o"
          # INC and OBJS are flag/object lists that must word-split.
          # shellcheck disable=SC2086
          gcc -Wall -Wextra -Werror $INC -o test_crypto \
            "$GITHUB_WORKSPACE/tests/unit/test_crypto.c" \
            "$GITHUB_WORKSPACE/src/ngx_http_autocert_crypto.c" \
            $OBJS -lssl -lcrypto
          ./test_crypto

  json-unit:
    name: JSON parser unit tests (M4c)
    needs: [resolve, build-nginx]
    runs-on: [self-hosted, builder02, lxc]
    timeout-minutes: 10
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Setup build deps
        uses: ./.github/actions/setup
        with:
          packages: build-essential libssl-dev

      - name: Download nginx build
        uses: actions/download-artifact@634f93cb2916e3fdff6788551b99b062d0335ce0 # v5
        with:
          name: build-nginx
          path: ${{ needs.resolve.outputs.nginx_dir }}

      - name: Build + run JSON unit tests
        run: |
          set -euo pipefail
          NGX="$GITHUB_WORKSPACE/${{ needs.resolve.outputs.nginx_dir }}"
          INC="-I$NGX/src/core -I$NGX/src/event -I$NGX/src/os/unix -I$NGX/objs"
          OBJS="$NGX/objs/src/core/ngx_palloc.o $NGX/objs/src/core/ngx_string.o \
                $NGX/objs/src/os/unix/ngx_time.o $NGX/objs/src/core/ngx_times.o \
                $NGX/objs/src/os/unix/ngx_alloc.o"
          # INC and OBJS are flag/object lists that must word-split.
          # shellcheck disable=SC2086
          gcc -Wall -Wextra -Werror $INC -o test_json \
            "$GITHUB_WORKSPACE/tests/unit/test_json.c" \
            "$GITHUB_WORKSPACE/src/ngx_autocert_json.c" \
            $OBJS
          ./test_json

  http-unit:
    name: HTTP parser unit tests (M4b)
    needs: [resolve, build-nginx]
    runs-on: [self-hosted, builder02, lxc]
    timeout-minutes: 10
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Setup build deps
        uses: ./.github/actions/setup
        with:
          packages: build-essential

      - name: Build + run HTTP parser unit tests
        run: |
          set -euo pipefail
          # Self-contained: the parser bodies are sliced from the shipped
          # ngx_autocert_acme.c by extract_http.sh and compiled against the
          # fuzz shim — no nginx objects needed (the rest of that TU is
          # event/SSL/resolver machinery the parser does not touch).
          bash tests/fuzz/extract_http.sh
          gcc -Wall -Wextra -Werror -I"$GITHUB_WORKSPACE" \
            -o test_http "$GITHUB_WORKSPACE/tests/unit/test_http.c"
          ./test_http

  challenge-unit:
    name: Challenge store unit tests (M5)
    needs: [resolve, build-nginx]
    runs-on: [self-hosted, builder02, lxc]
    timeout-minutes: 10
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Setup build deps
        uses: ./.github/actions/setup
        with:
          packages: build-essential

      - name: Download nginx build
        uses: actions/download-artifact@634f93cb2916e3fdff6788551b99b062d0335ce0 # v5
        with:
          name: build-nginx
          path: ${{ needs.resolve.outputs.nginx_dir }}

      - name: Build + run challenge store unit tests
        run: |
          set -euo pipefail
          NGX="$GITHUB_WORKSPACE/${{ needs.resolve.outputs.nginx_dir }}"
          INC="-I$NGX/src/core -I$NGX/src/event -I$NGX/src/os/unix -I$NGX/objs"
          # slab/rbtree/crc32/shmtx on top of the base pool/string/time objs.
          OBJS="$NGX/objs/src/core/ngx_palloc.o $NGX/objs/src/core/ngx_string.o \
                $NGX/objs/src/os/unix/ngx_time.o $NGX/objs/src/core/ngx_times.o \
                $NGX/objs/src/os/unix/ngx_alloc.o $NGX/objs/src/core/ngx_slab.o \
                $NGX/objs/src/core/ngx_rbtree.o $NGX/objs/src/core/ngx_crc32.o \
                $NGX/objs/src/core/ngx_shmtx.o"
          # The store source carries an (intentional) unused init-zone `data`
          # param, so compile it with nginx's own -Wall (no -Wextra), and keep
          # -Wextra -Werror on the test TU.
          # shellcheck disable=SC2086
          gcc -Wall -Werror $INC -c \
            "$GITHUB_WORKSPACE/src/ngx_autocert_challenge.c" -o challenge.o
          # shellcheck disable=SC2086
          gcc -Wall -Wextra -Werror $INC -c \
            "$GITHUB_WORKSPACE/tests/unit/test_challenge.c" -o test_challenge_tu.o
          # shellcheck disable=SC2086
          gcc -o test_challenge test_challenge_tu.o challenge.o $OBJS
          ./test_challenge

  alpn-unit:
    name: ALPN store unit tests (M10b)
    needs: [resolve, build-nginx]
    runs-on: [self-hosted, builder02, lxc]
    timeout-minutes: 10
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Setup build deps
        uses: ./.github/actions/setup
        with:
          packages: build-essential

      - name: Download nginx build
        uses: actions/download-artifact@634f93cb2916e3fdff6788551b99b062d0335ce0 # v5
        with:
          name: build-nginx
          path: ${{ needs.resolve.outputs.nginx_dir }}

      - name: Build + run ALPN store unit tests
        run: |
          set -euo pipefail
          NGX="$GITHUB_WORKSPACE/${{ needs.resolve.outputs.nginx_dir }}"
          INC="-I$NGX/src/core -I$NGX/src/event -I$NGX/src/os/unix -I$NGX/objs"
          OBJS="$NGX/objs/src/core/ngx_palloc.o $NGX/objs/src/core/ngx_string.o \
                $NGX/objs/src/os/unix/ngx_time.o $NGX/objs/src/core/ngx_times.o \
                $NGX/objs/src/os/unix/ngx_alloc.o $NGX/objs/src/core/ngx_slab.o \
                $NGX/objs/src/core/ngx_rbtree.o $NGX/objs/src/core/ngx_crc32.o \
                $NGX/objs/src/core/ngx_shmtx.o"
          # shellcheck disable=SC2086
          gcc -Wall -Werror $INC -c \
            "$GITHUB_WORKSPACE/src/ngx_autocert_alpn.c" -o alpn.o
          # shellcheck disable=SC2086
          gcc -Wall -Wextra -Werror $INC -c \
            "$GITHUB_WORKSPACE/tests/unit/test_alpn.c" -o test_alpn_tu.o
          # shellcheck disable=SC2086
          gcc -o test_alpn test_alpn_tu.o alpn.o $OBJS
          ./test_alpn

  cert-time-unit:
    name: Cert-expiry unit tests (M8)
    needs: [resolve, build-nginx]
    runs-on: [self-hosted, builder02, lxc]
    timeout-minutes: 10
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Setup build deps
        uses: ./.github/actions/setup
        with:
          packages: build-essential libssl-dev

      - name: Download nginx build
        uses: actions/download-artifact@634f93cb2916e3fdff6788551b99b062d0335ce0 # v5
        with:
          name: build-nginx
          path: ${{ needs.resolve.outputs.nginx_dir }}

      - name: Build + run cert-expiry unit tests
        run: |
          set -euo pipefail
          NGX="$GITHUB_WORKSPACE/${{ needs.resolve.outputs.nginx_dir }}"
          INC="-I$NGX/src/core -I$NGX/src/event -I$NGX/src/os/unix -I$NGX/objs"
          OBJS="$NGX/objs/src/core/ngx_palloc.o $NGX/objs/src/core/ngx_string.o \
                $NGX/objs/src/os/unix/ngx_time.o $NGX/objs/src/core/ngx_times.o \
                $NGX/objs/src/os/unix/ngx_alloc.o"
          # _GNU_SOURCE: the test cross-checks ngx_autocert_timegm against libc
          # timegm(3). The test include-shims the crypto .c to reach the static
          # ngx_autocert_timegm, so run it from the repo root (relative fixture
          # path tests/unit/fixture_leaf.pem).
          # shellcheck disable=SC2086
          gcc -D_GNU_SOURCE -Wall -Wextra -Werror $INC -o test_cert_time \
            "$GITHUB_WORKSPACE/tests/unit/test_cert_time.c" \
            $OBJS -lssl -lcrypto
          ./test_cert_time

  account-jsonsafe-unit:
    name: Account json_safe unit tests (M4d)
    needs: [resolve, build-nginx]
    runs-on: [self-hosted, builder02, lxc]
    timeout-minutes: 10
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Setup build deps
        uses: ./.github/actions/setup
        with:
          packages: build-essential

      - name: Download nginx build
        uses: actions/download-artifact@634f93cb2916e3fdff6788551b99b062d0335ce0 # v5
        with:
          name: build-nginx
          path: ${{ needs.resolve.outputs.nginx_dir }}

      - name: Build + run account json_safe unit tests
        run: |
          set -euo pipefail
          NGX="$GITHUB_WORKSPACE/${{ needs.resolve.outputs.nginx_dir }}"
          INC="-I$NGX/src/core -I$NGX/src/event -I$NGX/src/os/unix -I$NGX/objs"
          # Slice just ngx_autocert_account_json_safe from the shipped account
          # source — the function depends only on ngx_str_t, so no nginx objects
          # are linked.
          bash tests/unit/extract_jsonsafe.sh
          # shellcheck disable=SC2086
          gcc -Wall -Wextra -Werror -Itests/unit $INC -o test_account_jsonsafe \
            "$GITHUB_WORKSPACE/tests/unit/test_account_jsonsafe.c"
          ./test_account_jsonsafe

  # ------------------------------------------------------------ nginx+angie e2e
  # The full Pebble issuance/renewal suite, run against BOTH flavors via a
  # per-job matrix (needs build-nginx AND build-angie). fail-fast:false so an
  # nginx-leg failure doesn't cancel the angie leg and vice versa.

  # The full Pebble issuance/renewal suite runs against BOTH flavors via a
  # matrix: nginx uses build-nginx + nginx_dir, angie uses build-angie +
  # angie_dir. The dir is paired to the flavor through matrix.include so each
  # leg downloads the right build-<flavor> artifact.

  acme-directory:
    name: ACME account register over TLS (M4b/M4c/M4d) [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, docker]
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: Helper registers an ACME account over verified TLS
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: acme-directory.sh

  eab-account:
    name: EAB newAccount registration (M15) [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, docker]
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: Register an account with External Account Binding
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: eab-account.sh

  dns01-order:
    name: dns-01 order plumbing (D2, #16) [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, docker]
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: dns-01 order reaches the publish-TXT step
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: dns01-order.sh

  dns01-exec-hook:
    name: dns-01 exec-hook full issuance (D3, #16) [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, docker]
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: Full dns-01 issuance via operator exec hook against Pebble
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: dns01-exec-hook.sh

  wildcard-issue:
    name: wildcard (*.) issuance + serve (D4, #16) [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, docker]
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: Wildcard issuance via dns-01 hook + multi-subdomain serve
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: wildcard-issue.sh

  wildcard-shared:
    name: autocert_wildcard shared cert + suppression (D5, #16) [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, docker]
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: Concrete vhosts share one autocert_wildcard cert; covered names suppressed
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: wildcard-shared.sh

  multi-ca-grouping:
    name: multi-CA name grouping (M2, #15) [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, lxc]
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: server_names group by CA into ca_list (postconfig)
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: multi-ca-grouping.sh

  multi-ca-srv-scope:
    name: per-vhost multi-CA SRV-scope (M4, #15) [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, lxc]
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: per-vhost autocert_ca override -> distinct ca_list entry
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: multi-ca-srv-scope.sh

  multi-ca:
    name: multi-CA full issuance (M5, #15) [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, docker]
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: two CAs, two accounts, both vhosts issue (EAB on CA-B)
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: multi-ca.sh
          sudo: "true"

  multi-ca-contact:
    name: per-CA account contact (one mailto per CA) [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, docker]
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: each CA's newAccount carries its own vhost contact
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: multi-ca-contact.sh
          sudo: "true"

  single-process-reload:
    name: master_process off reload (driver + serve rebind) [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, docker]
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: single-process reload rebinds driver + serve to the new cycle
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: single-process-reload.sh

  reload-inflight:
    name: master_process off reload while ACME in flight (cancel_inflight) [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, docker]
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: a reload storm under in-flight ACME must not UAF (cancel_inflight)
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: reload-inflight.sh

  account-migration:
    name: per-CA account path + legacy migration (M3, #15) [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, lxc]
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: legacy flat account.key migrates to accounts/<ca_hash>/
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: account-migration.sh

  order-authz:
    name: ACME order + authz reaches valid (M6a) [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, docker]
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: Full order -> authz -> http-01 -> valid against Pebble
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: order-authz.sh
          sudo: "true"

  tls-alpn-issue:
    name: tls-alpn-01 full issuance, no port 80 (M10c) [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, docker]
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: Full tls-alpn-01 issuance against Pebble (port 80 never used)
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: tls-alpn-issue.sh

  rsa-issue:
    name: RSA-2048 full issuance (autocert_key_type rsa2048) [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, docker]
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: Issue an RSA-2048 certificate against Pebble, assert key algo
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: rsa-issue.sh

  dual-cert-issue:
    name: Dual-cert issuance (autocert_key_type p384 rsa2048) [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, docker]
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: Issue BOTH an EC and an RSA cert for one vhost against Pebble
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: dual-cert-issue.sh
          sudo: "true"

  dual-cert-serve:
    name: Dual-cert serving (EC-only vs RSA-only sigalgs) [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, docker]
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: Drop EC + RSA pairs, assert OpenSSL serves each by client sigalgs
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: dual-cert-serve.sh

  renewal:
    name: ACME renewal + multi-name issuance (M8) [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, docker]
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: Provision two domains, then force a renewal reissue
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: renewal.sh
          sudo: "true"

  backoff:
    name: Per-name failure backoff (M9b) [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, docker]
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: A failing name must back off, a good name issues once
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: backoff.sh
          sudo: "true"

  retry-after:
    name: 429 / Retry-After rate-limit awareness (M9c) [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, docker]
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: A 429 must honour Retry-After over the exponential backoff
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: retry-after.sh
          sudo: "true"

  mock-400-repoll:
    name: 400-on-respond -> re-poll -> finalize (mock CA, M6/#35) [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, docker]
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: A 400 on the challenge respond must re-poll, not abort the order
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: mock-400-repoll.sh
          sudo: "true"

  mock-finalize-ready:
    name: finalize-before-ready -> poll -> re-finalize (mock CA) [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, docker]
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: A finalize 400 (order not ready) must poll then re-finalize
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: mock-finalize-ready.sh
          sudo: "true"

  mock-download-400-retry:
    name: download 400 -> re-poll order -> retry download (mock CA) [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, docker]
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: A 400 on the certificate download must re-poll + retry, not abort
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: mock-download-400-retry.sh
          sudo: "true"

  mock-order-poll-retry:
    name: transient order poll -> re-poll, not abort (mock CA) [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, docker]
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: A transient order poll must re-poll + retry, not abort the order
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: mock-order-poll-retry.sh
          sudo: "true"

  mock-freshness-wrong-domain:
    name: wrong-domain stored leaf -> reissue (M2, mock CA) [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, docker]
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: A wrong-domain stored leaf must read as due and be reissued
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: mock-freshness-wrong-domain.sh
          sudo: "true"

  store-certbot:
    name: certbot store layout (live/<domain>/4 files) [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, docker]
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: autocert_store_layout certbot issues into live/<domain>/{privkey,cert,chain,fullchain}.pem
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: store-certbot.sh
          sudo: "true"

  store-symlink-swap:
    name: "store TOCTOU: planted-symlink component refused [${{ matrix.flavor }}]"
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, docker]
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: symlink at <store>/<domain> and <store>/live is refused, no write escapes the store
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: store-symlink-swap.sh
          sudo: "true"

  cert-validate-reject:
    name: pre-store cert validation rejects bad leaf (${{ matrix.cert_case }}) [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, docker]
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
            cert_case: key-mismatch
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
            cert_case: wrong-san
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
            cert_case: expired
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
            cert_case: future
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
            cert_case: key-mismatch
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
            cert_case: wrong-san
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
            cert_case: expired
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
            cert_case: future
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: invalid leaf is rejected and not stored
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: cert-validate-reject.sh
          sudo: "true"
          cert_case: ${{ matrix.cert_case }}

  ipv6-directory:
    name: IPv6-literal ACME directory [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, lxc]
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: IPv6 IP-SAN endpoint sends bracketed Host authority
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: ipv6-directory.sh

  ipv4-directory:
    name: IPv4-literal ACME directory [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, lxc]
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: IPv4 IP-SAN endpoint sends no SNI
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: ipv4-directory.sh

  config-rejects:
    name: config-time rejection of invalid directive values [${{ matrix.flavor }}]
    needs: [resolve, build-nginx, build-angie]
    runs-on: [self-hosted, builder02, docker]
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: autocert_dns_hook_timeout 0 (and negative) rejected at config time
        uses: ./.github/actions/run-e2e
        with:
          flavor: ${{ matrix.flavor }}
          dir: ${{ matrix.dir }}
          script: config-rejects.sh
          sudo: "false"

  slowloris-read:
    name: Slow-drip total-read budget (#37) [${{ matrix.flavor }}]
    needs: [resolve]
    runs-on: [self-hosted, builder02, docker]
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavor: nginx
            version: ${{ needs.resolve.outputs.nginx_version }}
            url: ${{ needs.resolve.outputs.nginx_url }}
            dir: ${{ needs.resolve.outputs.nginx_dir }}
          - flavor: angie
            version: ${{ needs.resolve.outputs.angie_version }}
            url: ${{ needs.resolve.outputs.angie_url }}
            dir: ${{ needs.resolve.outputs.angie_dir }}
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: Setup build deps
        uses: ./.github/actions/setup
        with:
          ccache-key: build-slowloris-${{ matrix.flavor }}-${{ matrix.version }}
      - name: Restore source tarball cache
        id: tarball-cache
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: ~/.cache/src-tarballs
          key: src-tarball-${{ matrix.flavor }}-${{ matrix.version }}
      - name: Fetch ${{ matrix.flavor }} ${{ matrix.version }}
        # Do NOT gate on cache-hit: on self-hosted runners actions/cache can
        # report a hit while the path is empty, so always ensure the file is
        # actually present (idempotent — curl only when missing).
        run: |
          set -euo pipefail
          mkdir -p ~/.cache/src-tarballs
          tarball=~/.cache/src-tarballs/${{ matrix.flavor }}-${{ matrix.version }}.tar.gz
          if [ ! -s "$tarball" ]; then
            curl -fsSL -o "$tarball" "${{ matrix.url }}"
          fi
      - name: Build ${{ matrix.flavor }} module with a tiny (2s) total-read budget
        run: |
          set -euo pipefail
          tar -xzf ~/.cache/src-tarballs/${{ matrix.flavor }}-${{ matrix.version }}.tar.gz
          cd "${{ matrix.dir }}"
          # -DNGX_AUTOCERT_READ_TOTAL=2000 shrinks the 60s budget to 2s so the
          # slow-drip abort is observable in a few seconds, not a minute.
          ./configure --with-compat --with-http_ssl_module \
            --with-cc="ccache cc" \
            --with-cc-opt="-Wall -Wextra -Werror -DNGX_AUTOCERT_READ_TOTAL=2000" \
            --add-dynamic-module="$GITHUB_WORKSPACE" > /tmp/conf.log 2>&1 \
            || { tail -40 /tmp/conf.log; exit 1; }
          make > /tmp/make.log 2>&1 || { tail -40 /tmp/make.log; exit 1; }
          test -f objs/ngx_http_autocert_module.so
          BIN=objs/${{ matrix.flavor }}; [ -x "$BIN" ] || BIN=objs/nginx
          test -x "$BIN"
      - name: A byte-dripping CA must be cut off by the total-read budget
        run: |
          set -euo pipefail
          NGX="$GITHUB_WORKSPACE/${{ matrix.dir }}"
          BIN="$NGX/objs/${{ matrix.flavor }}"; [ -x "$BIN" ] || BIN="$NGX/objs/nginx"
          SERVER_BIN="$BIN" NGX_BUILD_DIR="$NGX" \
            bash "$GITHUB_WORKSPACE/tests/e2e/slowloris-read.sh"
